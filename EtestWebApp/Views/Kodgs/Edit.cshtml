@model EtestWebApp.Models.Kodg

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Kodg</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Ktid" class="control-label"></label>
                <select asp-for="Ktid" class="form-control" asp-items="ViewBag.Ktid"></select>
                <span asp-validation-for="Ktid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Pid" class="control-label"></label>
                <select asp-for="Pid" class="form-control" asp-items="ViewBag.Pid"></select>
                <span asp-validation-for="Pid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Oid" class="control-label"></label>
                <select asp-for="Oid" class="form-control" asp-items="ViewBag.Oid"></select>
                <span asp-validation-for="Oid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
